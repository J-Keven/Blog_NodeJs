{{#each posts}}
<div class="card border-primary shadow-lg p-3 mb-5 bg-white rounded mt-3">
    <div class="card-body">
        <h3 class='card-title'>{{title}}</h3>
        <p>{{description}}</p>
        <a href="/admin/leiamais/{{_id}}">
            <button type="button" class="btn btn-success">Leia mais</button>
        </a>
    </div>
</div>

{{/each}}